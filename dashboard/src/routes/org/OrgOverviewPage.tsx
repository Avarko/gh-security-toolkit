import { Box, Typography } from "@mui/material";

export default function OrgOverviewPage() {
    return (
        <Box>
            <Typography variant="h4">Organization Overview</Typography>
            <Typography variant="body1" sx={{ mt: 2 }}>
                TODO: Show organization summary, apps, environments, usage metrics.
            </Typography>
        </Box>
    );
}
