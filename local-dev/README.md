# Local Development

This folder contains all scripts, configs, and data for local development and testing. Nothing here is used in production builds or CI pipelines.

- `data/` — Sample tenants, scan histories, run outputs
- `config/` — LocalStack and dashboard config files
- `scripts/` — Local simulation and setup scripts

See project root README for usage instructions.